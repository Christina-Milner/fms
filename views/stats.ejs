<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fen Model Show</title>
    <link rel="stylesheet" href="css/style.css">
    <script src="js/main.js" defer></script>
</head>

<body>
    <nav class="pageNav">
    <% if (isAuthenticated) { %>
        <%- include('partials/navAuth.ejs') %>
    <% } else { %>
            <%- include('partials/navNoAuth.ejs') %>
    <% } %>
    </nav>
        <h2>Number of participants</h2>
        <div class="numOfThings">
            <div>
                <p>Total:</p>
                <p>Juniors:</p>
                <p>Adults:</p>
            </div>
            <div>
                <p class="num"><%= info.length %></p>
                <p class="num"><%= info.filter(e => e.junior).length %></p>
                <p class="num"><%= info.filter(e => !e.junior).length %></p>
            </div>
        </div>
        <h2>Number of models entered</h2>
        <div class="numOfThings">
            <p class="num"><%= info.reduce((a, b) => a + b.numOfModels, 0) %></p>
        </div>
        <h2>Prizes awarded</h2>
        <div class="numOfThings">
            <div>
                <p>Highly Commended:</p>
                <p>Bronze:</p>
                <p>Silver:</p>
                <p>Gold:</p>
                <p>CORRR:</p>
            </div>
            <div>
                <p class="num"><%= info.filter(e => e.prizes.medal === "commended").length %></p>
                <p class="num"><%= info.filter(e => e.prizes.medal === "bronze").length %></p>
                <p class="num"><%= info.filter(e => e.prizes.medal === "silver").length %></p>
                <p class="num"><%= info.filter(e => e.prizes.medal === "gold").length %></p>
                <p class="num"><%= info.filter(e => e.prizes.corrr).length %></p>
            </div>
</body>